name: <tbs>
author: <tbs>
description: <tbs>
docker_image: <tbs>
entrypoints:
  example_entrypoint:
    description: <tbd>
    envs:
      LANGUAGE: de
    inputs:
      input_one:
        config:
          TEST: test
        description: <to-be-set>
        type: 'custom'
    outputs:
      output_one:
        config:
          OUT: out
        description: <to-be-set>
        type: 'custom'
  again_entrypoint:
    description: <tbd>
    envs:
      HI: null
